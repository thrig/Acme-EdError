Install via the cpan(1) command, or manually via:

perl Makefile.PL
make
make test
make install

Run perldoc(1) on the Acme::EdError module for more documentation.

http://github.com/thrig/Acme-EdError/tree/master
